general:
  artifacts:
    - build/chime.crx
    - build/chime.zip
dependencies:
  post:
    - npm install -g grunt-cli
test:
  override:
    - grunt test
  post:
    - echo $CHIME_KEY | sed 's/\\n/\n/g' > key.pem
    - grunt chrome-extension
    - grunt crx-extension