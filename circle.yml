general:
  artifacts:
    - build/chime.crx
    - build/chime.crx.zip
dependencies:
  post:
    - npm install -g grunt-cli
test:
  override:
    - grunt test
  post:
    - echo $CHIME_KEY > key.pem
    - grunt chrome-extension
    - grunt crx-extension